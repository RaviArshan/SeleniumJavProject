/*
package org.selenium;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.testng.Assert;*/
/*//*


public class FontColorAndFontSize {
    public static void main(String[] args) {
        WebDriver driver = null;

        String color = driver.findElement(By.id("myButton"))
                .getCssValue("color");
        System.out.println("Color: " + color); // usually in rgba format

// You can convert RGBA to Hex if needed and compare
        Assert.assertEquals(color, "rgba(0, 123, 255, 1)");


        String font = driver.findElement(By.id("myButton"))
                .getCssValue("font-family");
        Assert.assertTrue(font.contains("Arial"));
    }
}
*/
